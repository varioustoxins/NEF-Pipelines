
name: NEF-Pipelines

on:
  workflow_dispatch:
  push:
    branches: [ "master" ]
  pull_request:
  schedule:
    # Run roughly every 15 days at 00:00 UTC
    # (useful to check if updates on dependencies break the package)
    - cron: '0 0 1,16 * *'
permissions:
  contents: read

jobs:
  build:
    strategy:
      matrix:
        python-version: ["3.14"] #, "3.11", "3.12", "3.13"] # "3.14"]
        os: [ubuntu-latest] #, macos-latest] #windows-latest]

    runs-on: ${{ matrix.os }}
    steps:
#      - name: Support longpaths
#        run: git config --system core.longpaths true

      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv and set the python version
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install NEF-Pipelines from source
        run: |
          uv pip install --system -e ".[testing]"
          uv pip install --system streamfitter

      - name: Run nef test with debug flags
        run: |
          export PYTHONUNBUFFERED=1
          export PYTHONWARNINGS=default
          nef test -vvv

#          echo "==== Running tests with Python ${{ matrix.python-version }} ===="
#          echo "==== Python version ===="
#          python --version
#          echo "==== Pyparsing version ===="
#          python -c "import pyparsing; print(f'pyparsing {pyparsing.__version__}')"
#          echo "==== Running nef test ===="
#          nef test -vvv 2>&1 | tee test_output.txt || {
#            echo ""
#            echo "==== TEST FAILED WITH EXIT CODE $? ===="
#            echo ""
#            echo "==== Last 200 lines of test output ===="
#            tail -n 200 test_output.txt
#            echo ""
#            echo "==== First test failure (if grep finds it) ===="
#            grep -A 10 "FAILED\|ERROR\|error:" test_output.txt | head -n 50 || echo "No clear failure pattern found"
#            exit 1
#          }
